# 白泽 v0.3 代码审查清单

**审查人**: 暗香
**日期**: 2026-05-28
**改动者**: 雪瑶

---

## 一、改动概览

### 新增功能
1. **API 配置剪贴板导入导出** — 通过剪贴板传递 JSON 配置，零门槛
2. **长按消息删除（带确认）** — 长按消息气泡弹出删除确认对话框
3. **设置页对话管理** — 导出/导入/清空对话记录按钮

### 修复/优化
4. **ClusterInferenceProvider 占位化** — 移除对不存在的 lancluster-core 依赖
5. **lancluster-core 依赖注释** — build.gradle.kts 中注释掉该依赖

---

## 二、文件改动详情

### 1. ClipboardConfigHelper.kt（新建）

**路径**: pp/src/main/java/com/baize/ai/util/clipboardconfighelper.kt

**功能**: 剪贴板 JSON 配置解析/生成/校验

**审查要点**:
- [ ] JSON 格式是否合理
- [ ] 错误处理是否完善
- [ ] 默认值是否正确
- [ ] 安全性（API Key 处理）

**JSON 格式**:
`json
{
  "apiKey": "xxx",
  "model": "MiniMax-M2.7-highspeed",
  "baseUrl": "https://api.minimax.chat/v1"
}
`

---

### 2. activity_settings.xml（修改）

**路径**: pp/src/main/res/layout/activity_settings.xml

**改动**:
- 新增"从剪贴板导入"和"导出到剪贴板"按钮（粉色/紫色）
- 新增"导出对话"、"导入对话"、"清空对话"按钮（蓝色/绿色/红色）

**审查要点**:
- [ ] 按钮布局是否合理
- [ ] 颜色区分是否清晰
- [ ] 间距和对齐

---

### 3. SettingsActivity.kt（修改）

**路径**: pp/src/main/java/com/baize/ai/ui/settings/settingsactivity.kt

**改动**:
- 新增 import: ClipboardConfigHelper, Context
- 新增按钮声明/初始化/点击监听
- 新增 importFromClipboard() — 从剪贴板导入 API 配置
- 新增 xportToClipboard() — 导出 API 配置到剪贴板
- 新增 xportChat() — 导出对话到剪贴板
- 新增 importChat() — 从剪贴板导入对话（有确认）
- 新增 clearChat() — 清空对话（有确认，不可恢复提示）

**审查要点**:
- [ ] ViewModel 获取方式是否正确
- [ ] 确认对话框逻辑
- [ ] 错误提示是否友好
- [ ] 剪贴板访问权限

---

### 4. mainactivity.kt（修改）

**路径**: pp/src/main/java/com/baize/ai/ui/mainactivity.kt

**改动**:
- ChatBubble 函数新增 onDelete 回调和 messageIndex 参数
- Surface 添加 combinationClickable 修饰符（长按触发）
- LazyColumn 改为按 index 遍历，传入 onDelete 和 messageIndex
- 新增 showDeleteConfirm 和 pendingDeleteIndex 状态变量
- 新增删除确认 AlertDialog

**审查要点**:
- [ ] combinationClickable 导入是否正确
- [ ] 状态变量作用域
- [ ] Dialog 位置是否在 BaizeChatScreen 内
- [ ] 删除逻辑是否正确（index 映射）
- [ ] 长按和点击冲突处理

---

### 5. clusterinferenceprovider.kt（重写为占位）

**路径**: pp/src/main/java/com/baize/ai/inference/clusterinferenceprovider.kt

**改动**:
- 移除 lancluster-core 依赖（GrpcClient, InferenceRequest, InferenceStatus）
- 改为 stub 实现，所有方法返回 failure
- 保留类结构和接口签名

**审查要点**:
- [ ] 接口签名是否与 InferenceProvider 一致
- [ ] 返回的错误信息是否清晰
- [ ] 是否影响其他代码

---

### 6. build.gradle.kts（修改）

**路径**: pp/build.gradle.kts

**改动**:
- 注释掉 implementation(project(":lancluster-core"))

**审查要点**:
- [ ] 是否有其他地方依赖此模块
- [ ] 注释而非删除，保留恢复可能

---

## 三、编译状态

- ✅ BUILD SUCCESSFUL
- ⚠️ 有 deprecation warnings（VolumeUp → AutoMirrored, Divider → HorizontalDivider），非本次引入

---

## 四、测试建议

1. **API 配置剪贴板导入导出**
   - 复制 JSON → 点"从剪贴板导入" → 检查填充
   - 填好配置 → 点"导出到剪贴板" → 粘贴验证

2. **长按消息删除**
   - 长按消息 → 确认对话框 → 删除
   - 长按消息 → 取消 → 消息保留

3. **对话管理**
   - 导出对话 → 剪贴板粘贴验证
   - 导入对话 → 检查消息数
   - 清空对话 → 确认后消息清空

---

**审查完成**: _____________ 日期: _____________
